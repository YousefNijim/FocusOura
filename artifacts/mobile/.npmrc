# lucide-react-native 0.470 declares a peer of React <= 18 while this app runs
# React 19. pnpm only warned about it, so the app has always been built this
# way; npm refuses by default. Recording the tolerance here keeps installs
# reproducible instead of depending on whoever remembers the flag.
legacy-peer-deps=true
